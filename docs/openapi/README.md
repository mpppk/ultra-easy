# OpenAPI 3.1

`openapi.yaml` is the root document. Path Items and schemas are split into external `$ref` files so that the specification stays reviewable in Git.

This is a semantics-preserving modularization of the single-document OpenAPI contract migrated from Google Docs.
